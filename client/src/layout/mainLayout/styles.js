import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
    container: {
        marginTop: 100,
        padding: '40px 100px',
        maxWidth: '1000px'
    },
}))